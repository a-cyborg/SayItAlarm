name: Build Android

on:
  push:
  workflow_dispatch:

jobs:
  build-and-unit-test:
    name: "Build and Local tests"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy ci-gradle.properties
        run: mkdir -p ~/.gradle ; cp .github/ci-gradle.properties ~/.gradle/gradle.properties

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          validate-wrappers: true
          gradle-home-cache-cleanup: true

      - name: Validate gradle-wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Check build-logic
        run: ./gradlew check -p build-logic

      - name: Spotless check
        run: ./gradlew spotlessCheck --init-script=gradle/spotless.gradle.kts

      - name: Build and run unit test
        run: ./gradlew build --stacktrace --scan --no-daemon

      - name: Roborazzi compare
        run: ./gradlew compareRoborazziDebug

      - name: Upload roborazzi screenshot test Result
        uses: actions/upload-artifact@v4
        with:
          name: screenshot-test-results
          path: '**/src/test/snapshots/roborazzi/images/*_compare.png'

      - name: Upload test result
        uses: actions/upload-artifact@v4
        with:
          name: local-test-results
          path: '**/build/reports/tests/test*UnitTest/**.html'

      - name: Upload lint result
        uses: actions/upload-artifact@v4
        with:
          name: lint-result
          path: '**/build/reports/lint-results-*.html'

  android-test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        api-level: [26, 30]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          validate-wrappers: true
          gradle-home-cache-cleanup: true

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules  
          sudo udevadm control --reload-rules  
          sudo udevadm trigger --name-match=kvm  

      - name: Cache AVD
        uses: actions/cache@v4
        id: avd-cache
        with:
          path: |
            ~/.android/avd/*  
            ~/.android/adb*
          key: avd-${{ matrix.api-level }}

      - name: Run Android Test
        if: steps.avd-cache.outputs.cache-hit != 'true'
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: ${{ matrix.api-level }}
          arch: x86_64
          disable-animations: true
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: ./gradlew connectedAndroidTest